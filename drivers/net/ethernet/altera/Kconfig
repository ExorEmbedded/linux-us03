config ALTERA_TSE
	tristate "Altera Triple-Speed Ethernet MAC support"
	depends on HAS_DMA
	select PHYLIB
	---help---
	  This driver supports the Altera Triple-Speed (TSE) Ethernet MAC.

	  To compile this driver as a module, choose M here. The module
	  will be called alteratse.
config ALTERA_TSE_PCIE
	bool "PCIe support for the Altera Triple-Speed Ethernet MAC"
	depends on ALTERA_TSE
	---help---
	  This option builds the PCIe version of the Altera TSE driver.
